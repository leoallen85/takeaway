class Menu
  attr_reader :dishes

  def initialize(dishes)
    @dishes = dishes
  end

  def print
    dishes.map do |title, price|
      "%s Å£%.2f" % [title.to_s.capitalize, price]
    end.join(", ")

jjj    diesh
  end

  def dish_entries
      dishes.map do ||
      		 yield title, price
  end
end
